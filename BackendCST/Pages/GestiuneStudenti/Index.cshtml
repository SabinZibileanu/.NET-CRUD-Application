@page
@model BackEndCST.Pages.GestiuneStudenti.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Students[0].nume)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Students[0].an)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Students[0].grupa)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Students) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nume)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.an)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.grupa)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.id_student">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.id_student">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.id_student">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
